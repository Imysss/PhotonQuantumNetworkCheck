# 매칭 진입 전 (Pre-match) 체크리스트

매칭 시작 함수 / 매칭 버튼 흐름 PR 점검. ID는 [references/G-pre-match.md](../references/G-pre-match.md) 참조.

## 사전 검사

- [ ] **G1** 매칭 시작 함수 진입 즉시 검사 체인 실행 (UI 활성 조건만으로 우회 못 하게).
- [ ] **G1** 스태미너/에너지 부족 → 토스트 + 충전 팝업, 매칭 시작 안 함.
- [ ] **G1** 앱·콘텐츠 버전 검사 (서버 조회) → 강제 업데이트 팝업.
- [ ] **G1** 시즌 데이터 로드 여부 검증 → 미로드 시 기본값 또는 재로드.
- [ ] **G1** 초대 코드 입장 시 코드 비어있음 검사.
- [ ] **G1** 각 실패에 별도 텔레메트리 이벤트.

## 자원 차감

- [ ] **G2** 차감 시점은 양쪽 ready + 씬 전환 직전.
- [ ] **G2** 매칭 실패 / 취소 시 차감 안 됨 보장.
- [ ] **G2** 차감 후 매칭 실패(B4/B5) 케이스용 idempotent 환불 API.
- [ ] **G2** 차감/환불/실패 텔레메트리 분리.

## 데이터 sanitization

- [ ] **G3** 닉네임 송신 직전 sanitize (제어 문자, `\`, 줄바꿈, 길이 제한).
- [ ] **G3** 받는 쪽도 sanitize 한 번 더 (신뢰 금지).
- [ ] **G3** 채팅 등 모든 사용자 입력 텍스트 동일 처리.

## Mutex / 중복 클릭

- [ ] **G4** 매칭 시작 함수에 `_isStartingMatch` 가드.
- [ ] **G4** 매칭 버튼 클릭 직후 `interactable = false`.
- [ ] **G4** 매칭 완료/실패/취소 후 버튼 재활성화.
- [ ] **G4** A10(disconnect 진행 중 재시작)과 짝으로 ClientState 가드.
